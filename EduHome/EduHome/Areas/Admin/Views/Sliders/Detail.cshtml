@model Slider
<div>
    <img src="~/img/slider/@Model.Image" style="width:400px; height:200px; object-fit:cover" />
    <h2 style="margin-top:10px">@Model.Title</h2>
    <div>@Model.Description</div>
    <a asp-action="Index" class="btn btn-secondary mt-3">Go Back</a>
</div>
